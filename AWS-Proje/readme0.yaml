    0 CREATE and admin user and AN EC2 intance to managed the EKS cluster. 
    1  aws --version
    2  curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
    3  unzip awscliv2.zip
    4  which aws
    5  sudo ./aws/install --bin-dir /usr/bin --install-dir /usr/bin/aws-cli --update
    6  aws --version
    7  aws
    8  aws configure
    9  curl -o kubectl https://amazon-eks.s3.us-west-2.amazonaws.com/1.21.2/2021-07-05/bin/linux/amd64/kubectl
   10  chmod +x ./kubectl
   11  mkdir -p $HOME/bin && cp ./kubectl $HOME/bin/kubectl && export PATH=$PATH:$HOME/bin
   12  echo 'export PATH=$PATH:$HOME/bin' >> ~/.bashrc
   13  kubectl version --short --client
   14  curl --silent --location "https://github.com/weaveworks/eksctl/releases/latest/download/eksctl_$(uname -s)_amd64.tar.gz" | tar xz -C /tmp
   15  sudo mv /tmp/eksctl /usr/local/bin
   16  eksctl version
   
   # BUNU DENE  
   17  eksctl create cluster --name dev --version 1.21 --region us-east-1 --nodegroup-name standard-workers --node-type m5.large --nodes 4 --nodes-min 1 --nodes-max 4 --managed
  
   18  eksctl get cluster
   19  aws eks update-kubeconfig --name dev --region us-east-1